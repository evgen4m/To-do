<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esoft.devtodolist">

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DevTodoList.Blu">
        <activity
            android:name=".activity.settingsActivity.SettingsActivity"
            android:exported="true" />
        <activity
            android:name=".activity.aboutActivity.AboutActivity"
            android:exported="true" />
        <activity android:name=".activity.newNoteActivity.NewNoteActivity" />
        <activity
            android:name=".activity.splashActivity.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.noteListActivity.NoteListActivity" />
    </application>

</manifest>